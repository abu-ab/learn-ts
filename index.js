function greed(person, date) {
    return "asd " + person + ", " + date.toDateString();
}
greed("gray", new Date());
// string number boolean
var str = '123';
var num = 223;
var flag = false;
var arr = [1, 23, 23];
var a;
function greet(name) {
    return name;
}
greet('123');
function printPoort(point) {
    return point;
}
printPoort({ x: 1, y: 2 });
